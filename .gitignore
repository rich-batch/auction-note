# Hugo
/public/
/resources/
.hugo_build.lock

# Claude Code 로컬 설정 (개인별 권한 등)
.claude/settings.local.json

# 백업 파일
*.backup
keywords.backup-*.csv
pipeline/guide/keywords.backup-*.csv

# 서류 원본(매각물건명세서·등기 PDF 등) — 실명이 들어 있어 저장소에 올리지 않는다
/private/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
.AppleDouble
.LSOverride

# 임시 파일
*.tmp
*.temp

# 자동 글쓰기 스케줄러 로그, launchd 설정(이 컴퓨터의 절대 경로가 들어 있음)
tools/auto/*.log
tools/auto/*.plist
